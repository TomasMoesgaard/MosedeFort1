using UnityEngine;
using System.Collections;

public class RandomSound : MonoBehaviour {

    /*
	public int minRandom = 5;

	public int maxRandom = 13;

	public GameObject camera;

	private ScreenClick sc;

	private int delay;

	// Use this for initialization
	void Start () {
	
		sc = camera.GetComponent<ScreenClick>();

	}
	
	// Update is called once per frame
	void Update () {
	
		if(!GetComponent<AudioSource>().isPlaying && !sc.Playing){
			delay = Random.Range(minRandom, maxRandom);
			GetComponent<AudioSource>().PlayDelayed((float)delay);
		}

		if(sc.Playing){
			GetComponent<AudioSource>().volume = 0.0f;}
		else{
			GetComponent<AudioSource>().volume = 1.0f;}

	}
    */
}
