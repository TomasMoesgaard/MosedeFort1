using UnityEngine;
using System.Collections;

public class AnimationOrderScript : MonoBehaviour {

/*
	public GameObject go;
	
	private Control control;

	public GameObject ambientSource;
	
	private ZeppelinerSound zepSound;
	
	private bool doneOnce = false;

	protected Animator avatar;
	
	// Use this for initialization
	void Start () {

		avatar = GetComponent<Animator>();

		control = go.GetComponent<Control>();




		if(ambientSource != null)
		zepSound = ambientSource.GetComponent<ZeppelinerSound>();

	
	}
	
	// Update is called once per frame
	void Update () {


		if(control.AudioDone && !doneOnce || Input.GetKeyDown(KeyCode.A)){
			avatar.SetTrigger("trig");
			doneOnce = true;
			if(ambientSource != null){
				zepSound.playNow = true;
			}
			else
			{
				GetComponent<AudioSource>().PlayDelayed(1);

			}


	}


	
	}
    */
}
